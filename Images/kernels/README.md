# Images/kernels/

Programmable NxN signed 8-bit kernel coefficient files (e.g. `.txt`/`.mem`),
generated/consumed by `Python/image_generator.py` and `Python/image_loader.py`,
and loaded into `RTL/kernel_memory.v` for simulation and hardware testing.
Populated starting Phase 2.
